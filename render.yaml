services:
  - type: web
    name: 2027-election-evidence
    runtime: python
    plan: free
    buildCommand: python -m pip install -r requirements-dev.txt
    startCommand: python tools/evidence_product.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
